// @ts-check
// 린트랑 포매팅, 코드를 짜는 중간중간 피드백을 준다, 모두가 같은 규칙을 쓰기때문에, 포맷때문에 팀원들끼리 충돌이 있을 이유가 없다
// Formattig, Linting, Type Checking
// Formatting: Prettier
// Linting: ESLint
// Type checking: TypeScript
/* eslint-disable */

const { path, paths, filename } = module

console.log({
  path,
  paths,
  filename,
})

// CommonJS: require
// ECMAScript: export, import
